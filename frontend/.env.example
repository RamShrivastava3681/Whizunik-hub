# Frontend Environment Variables
# Copy this file to .env.local and update the values for your environment

# API Base URL - Update this for your production backend
VITE_API_URL=http://localhost:5003/api

# Backend Environment Variables (for server.cjs)
# Set these when deploying to production

# Node Environment
NODE_ENV=production

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/whizunik-portal

# Email Configuration (Required for email verification to work)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# JWT Secret (Generate a strong secret for production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Port
PORT=5003